{
  "name": "workhub-dashboard",
  "version": "1.0.0",
  "description": "Clean dashboard for work and personal projects — runs as a Windows desktop app via Electron",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win --x64",
    "dist": "electron-builder --win --x64",
    "pack": "electron-builder --dir",
    "test": "node --check main.js && node --check renderer/app.js && echo Syntax OK"
  },
  "devDependencies": {
    "electron": "^40.6.0",
    "electron-builder": "^26.8.1"
  },
  "build": {
    "appId": "com.workhub.dashboard",
    "productName": "WorkHub",
    "icon": "assets/icon.ico",
    "directories": {
      "output": "release"
    },
    "files": [
      "dashboard.html",
      "main.js",
      "package.json",
      "renderer/",
      "assets/"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "assets/icon.ico"
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "WorkHub"
    }
  }
}
